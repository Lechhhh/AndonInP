{
  "name": "andon-client",
  "version": "1.0.0",
  "description": "Klient desktop systemu Andon (Electron) — łączy się z serwerem Andon w sieci lokalnej",
  "main": "main.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "start": "electron .",
    "dist": "electron-packager . AndonClient --platform=win32 --arch=x64 --out=dist --overwrite --prune=true"
  },
  "devDependencies": {
    "electron": "44.3.0",
    "@electron/packager": "20.3.0"
  }
}
